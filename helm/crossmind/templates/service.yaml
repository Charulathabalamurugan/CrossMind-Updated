apiVersion: v1
kind: Service
metadata:
  name: {{ include "crossmind.fullname" . }}
  labels:
    {{- include "crossmind.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "crossmind.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
